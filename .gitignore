# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Node (in case anyone adds a dev server later)
node_modules/
package-lock.json

# Build / dist
dist/
build/

# Environment
.env
.env.local

# Datos privados — NUNCA se suben ni se publican
# Correos reales de los vendors importados (datos personales de terceros).
data/vendors-contacts.local.js
# Claves de servicio de Firebase (la config web SÍ es pública; estas NO).
firebase-service-account*.json
# Exportaciones de la hoja de cálculo original de vendors (llevan correos reales)
*.xlsx
vendors-export*.csv
