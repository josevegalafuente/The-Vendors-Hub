/* =========================================================================
   PLANTILLA — copia este archivo como  data/vendors-contacts.local.js
   y rellénalo con los correos reales. El archivo .local.js está en
   .gitignore, así que nunca se sube al repositorio ni se publica.

   La clave es el campo "ref" de cada vendor en data/vendors-data.js.
   ========================================================================= */
window.VENDOR_CONTACTS = {
  // "all-property-maintenance-services-llc": "contacto@ejemplo.com",
  // "american-home-maintenance-llc":         "otro@ejemplo.com"
};
